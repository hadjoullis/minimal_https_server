# minimal_https_server
- Simple multithreaded https server using TLS
- support for the following request types:
    * GET
    * HEAD
    * POST
    * DELETE

## BUILDING
Install dependencies:
- openssl

Run:
```
make
make debug    # build with debug info
```
